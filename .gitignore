node_modules/
dist/

# opencode's runtime/project config. The plugin's *source* assets live in the
# top-level command/ skills/ agent/ dirs and are copied INTO a user's config on
# install — this repo must never carry a .opencode/ of its own.
.opencode/

# opencode-ultra runtime output: per-run workflow journals (resume state) and
# any other plugin-written state. Generated, not source.
.opencode-ultra/

# scratch
*.tar.gz
